<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raden.sisforupa">
    <!--        android:theme="@style/AppTheme"-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".TentangActivity"></activity>
        <activity android:name=".SaranActivity" />
        <activity android:name=".Ikon1Activity" />
        <activity android:name=".Ikon2Activity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".NilaiActivity" />
        <activity android:name=".CuacaActivity" />
        <activity android:name=".SimulasinilaiActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>